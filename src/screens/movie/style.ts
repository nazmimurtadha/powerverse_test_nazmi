import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        backgroundColor: '#476fbf',
        padding: 10,
        marginVertical: 2,
        marginHorizontal: 16,
    },
    title: {
        color: '#ffffff',
        fontSize: 12,
    },
});
